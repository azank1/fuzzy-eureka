/**
 * Core constants for orchestration system
 * Phase 3.5 Day 2
 */

export const MAX_PLAN_STEPS = 12;
export const WS_HEARTBEAT_MS = 30_000;   // heartbeat ping
export const WS_IDLE_CLOSE_MS = 60_000;  // auto-close idle sockets
export const RUN_HISTORY_LIMIT = 100;
